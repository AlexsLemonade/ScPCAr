# Check if the ScPCA API is reachable

This function performs a simple GET request to the ScPCA API to verify
that it is reachable.

## Usage

``` r
check_api()
```

## Value

TRUE if the API is reachable, otherwise an error is raised.
