# Get the base filename from a ScPCA portal download URL

(this may become obsolete if we get download filenames in the API
response)

## Usage

``` r
parse_download_file(scpca_url)
```

## Arguments

- scpca_url:

  The ScPCA portal download URL

## Value

the download filename
