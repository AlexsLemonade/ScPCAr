# Fetch a single CCDL dataset by ID

Fetch a single CCDL dataset by ID

## Usage

``` r
get_ccdl_dataset_detail(id, auth_token)
```

## Arguments

- id:

  CCDL dataset ID

- auth_token:

  API authentication token

## Value

the dataset detail as a list
